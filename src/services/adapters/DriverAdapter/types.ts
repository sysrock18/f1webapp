export type RawDriver = {
  driverId: string;
  code: string;
  url: string;
  givenName: string;
  familyName: string;
};

export type Driver = {
  id: string;
  code: string;
  url: string;
  name: string;
};
